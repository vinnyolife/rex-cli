# RL Shell V1 Benchmark

This directory holds the isolated synthetic bugfix benchmark for the shell RL v1 experiment.

- `configs/benchmark-v1.json`: benchmark generation config and split gates
- `seeds/*`: seed repositories used to materialize generated tasks
- `tasks/generated/*`: generated benchmark tasks
- `runs/*`: experiment outputs written by the runner
- `campaigns/*`: campaign-level summaries
